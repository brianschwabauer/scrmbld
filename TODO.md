* [x] If you are already signed in and try to go to /signin, it should take you to the /account page
* [x] Change better-auth.session_token and better-auth.state cookies to something scrmbld specific.
* [ ] Instead of having a red "Remove" button for the friends list, use a red trash icon with a tooltip saying "Remove friend" or "Remove friend invite".
* [ ] Make the "Link History to My Account" button have the same 3d effect as the other buttons.
* [ ] On the /results page (after finishing a game), add the ability to sign in from that page (if not already signed in). If the user is already signed in, add a link to their profile page so they can see their full game history. If they aren't signed in, they can click sign in and go through the sign in / sign up process. If they do this, it should automatically import the game history for their scrmbld_user_uuid.
* [ ] Fix styling of the /user/[id] page. The contrast doesn't work well. Also, it should not have a profile picture circle. It should also display the user's display name (if set in their account). It should show both the display name and the username. This page should also be more mobile friendly.
* [ ] Add a link to the user's profile (/user/[username_or_id]) page in their account settings. This should probably be at the top, before any settings are shown.
* [ ] Add a button on the /account page that takes the user back to the home page (maybe use the same button as the newsletter signup page?)
